# Cloudinary Upload Preset 생성 가이드

## ⚡ 빠른 설정 (2분)

Upload Preset은 업로드 설정을 미리 정의하는 것입니다.

---

## 단계별 가이드

### 1. Settings 페이지 접속

Cloudinary Console에서:
- 왼쪽 메뉴 → **Settings** (⚙️ 아이콘) 클릭

### 2. Upload 탭 선택

- 상단 탭에서 **Upload** 클릭

### 3. Upload Presets 섹션 찾기

- 페이지를 아래로 스크롤
- **Upload presets** 섹션 찾기

### 4. 새 Preset 추가

- **Add upload preset** 버튼 클릭

### 5. 설정 입력

**필수 설정:**

```
Upload preset name: jsblind_products
```

**Signing Mode (매우 중요!):**
```
⚠️ Unsigned 선택 (Signed가 아님!)
```

**Folder:**
```
products
```

**선택 설정 (권장):**

- **Allowed formats**: `jpg, png, gif, webp`
- **Max file size**: `10485760` (10 MB)
- **Unique filename**: ✅ 체크

### 6. 저장

- 하단의 **Save** 버튼 클릭

---

## ✅ 확인

Upload Preset이 생성되면 목록에 다음과 같이 표시됩니다:

```
Name: jsblind_products
Mode: Unsigned
Folder: products
```

---

## 🚀 다음 단계

1. **서버 재시작**
   ```bash
   # 터미널에서 Ctrl+C 누른 후
   npm run dev
   ```

2. **테스트**
   - Admin → Products → Add New Product
   - Product Image 섹션에서 이미지 업로드
   - "Image uploaded successfully to Cloudinary" 메시지 확인

---

## ❓ 문제 해결

### "Upload preset not found" 오류

**원인**: Preset 이름이 다르거나 생성되지 않음

**해결**:
1. Cloudinary Console에서 Preset 이름 확인
2. 정확히 `jsblind_products`인지 확인
3. 대소문자 구분 주의

### "Must use unsigned upload preset" 오류

**원인**: Signing Mode가 "Signed"로 설정됨

**해결**:
1. Upload Preset 편집
2. Signing Mode를 **Unsigned**로 변경
3. Save

---

## 📸 스크린샷 참고

Upload Preset 설정 화면은 다음과 같이 보입니다:

```
┌─────────────────────────────────────┐
│ Upload preset name                  │
│ jsblind_products                    │
├─────────────────────────────────────┤
│ Signing Mode                        │
│ ○ Signed                            │
│ ● Unsigned  ← 이것을 선택!          │
├─────────────────────────────────────┤
│ Folder                              │
│ products                            │
├─────────────────────────────────────┤
│ [Save]                              │
└─────────────────────────────────────┘
```

---

완료하셨다면 이미지 업로드가 작동합니다! 🎉
